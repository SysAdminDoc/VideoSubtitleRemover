# Models Folder

Runtime model weights and OCR assets can be cached here when a backend chooses
to persist them locally.

- These files may be downloaded on demand by the processing stack.
- Large model binaries are intentionally ignored by git.
- Keep `README.txt` so the folder remains visible in fresh clones and packaged
  builds.
